<h1>Workshops 2021</h1>

<% @files.drop(2).each do | file_name |  %>
  <%# Usamos lo siguiente para quitar la extensión al nombre del archivo %>
  <a href="/<%= File.basename(file_name, ".*") %> "><%= File.basename(file_name, ".*") %> </a> <br>
<% end %>

<br><a href="/create">Create a new workshop</a>
